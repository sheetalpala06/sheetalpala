I’m going to scaffold a full Terraform configuration in your workspace with secure defaults (Linux VM + SSH key auth), then I’ll run the Day-4 gate commands locally to verify formatting, validation, plan behavior, and idempotency readiness. I’ll first inspect the workspace and current guide file so I can place files cleanly.

Read [](file:///c%3A/Users/labuser/Documents/training/capstone-project)

Searched for files matching `**/*`, no matches

I confirmed the workspace is empty, so next I’ll create a production-style Terraform baseline (`main.tf`, `variables.tf`, `outputs.tf`) with the requested naming, tags, and security controls, then run the Day-4 gate commands.

Read memory

Created [](file:///c%3A/Users/labuser/Documents/training/capstone-project/variables.tf)

Created [](file:///c%3A/Users/labuser/Documents/training/capstone-project/main.tf)

Created [](file:///c%3A/Users/labuser/Documents/training/capstone-project/outputs.tf)

I’ve created the three Terraform files with secure Linux-first architecture and constraints; next I’ll execute the Day-4 gate commands in sequence to verify the configuration and capture any fixes needed.

Ran terminal command: terraform version

Checked [](file:///c%3A/Users/labuser/Documents/training/capstone-project/main.tf), [](file:///c%3A/Users/labuser/Documents/training/capstone-project/variables.tf), [](file:///c%3A/Users/labuser/Documents/training/capstone-project/outputs.tf), no problems found

Ran terminal command: terraform init
Completed with input: terraform init

I found a Terraform language constraint: `lifecycle.prevent_destroy` must be a literal, not a variable. I’ll patch main.tf to use a literal `true` on critical resources so initialization and gates can proceed.

Generating patch (22 lines) in [](file:///c%3A/Users/labuser/Documents/training/capstone-project/main.tf)


```

```


Generating patch (11 lines) in [](file:///c%3A/Users/labuser/Documents/training/capstone-project/variables.tf)


```

```
I’ve patched the lifecycle issue; next I’ll rerun initialization, then execute the lint gate (`fmt`, `validate`) and report exact outcomes.

Ran terminal command: terraform init

Ran terminal command: terraform fmt -recursive; terraform validate

Lint gate is passing. I’ll now attempt the dry-run and idempotency gates by supplying runtime variables; first I’ll detect your Azure subscription context and create a temporary SSH key pair if needed.

Ran terminal command: az account show --query id -o tsv

Ran terminal command: if (-not (Test-Path ".\\finbridge-temp-key")) { ssh-keygen -t ed25519 -f .\\finbridge-temp-key -N "" | Out-Null }; Get-Content .\\finbridge-temp-key.pub
Completed with input: if (-not (Test-Path ".\\finbridge-temp-key")) { ssh-keygen -t ed25519 -f .\\finbridge-temp-key -N "" | Out-Null }; Get-Content .\\finbridge-temp-key.pub

Ran terminal command: (Invoke-RestMethod -Uri "https://api.ipify.org").ToString()

Ran terminal command: $pub = Get-ChildItem "$HOME\.ssh\*.pub" -ErrorAction SilentlyContinue | Select-Object -First 1; if ($null -eq $pub) { Write-Error "No SSH public key found under $HOME/.ssh" } else { Get-Content $pub.FullName }

I retrieved your subscription ID, current public IP, and an existing SSH public key; I’ll now create a local `terraform.tfvars` to run the dry-run and idempotency gates exactly as requested.

Created [](file:///c%3A/Users/labuser/Documents/training/capstone-project/terraform.tfvars)

Ran terminal command: terraform plan -out=tfplan

Read [](file:///c%3A/Users/labuser/AppData/Roaming/Code/User/workspaceStorage/2173fc5dcc1b78ccf2ef22396b6c822a/GitHub.copilot-chat/chat-session-resources/717979f4-4683-409a-9eee-7e9f0d4feb8c/call_ihrnSEYUBYAEhYh0ZA64VmBf__vscode-1781849615722/content.txt#1-1), lines 1 to 260

Read [](file:///c%3A/Users/labuser/AppData/Roaming/Code/User/workspaceStorage/2173fc5dcc1b78ccf2ef22396b6c822a/GitHub.copilot-chat/chat-session-resources/717979f4-4683-409a-9eee-7e9f0d4feb8c/call_ihrnSEYUBYAEhYh0ZA64VmBf__vscode-1781849615722/content.txt#261-261), lines 261 to 340

Ran terminal command: terraform apply -auto-approve tfplan

Ran terminal command: terraform plan

Provisioning completed successfully and all resources were created; the final idempotency re-plan was skipped, so I’ll provide the exact command to run for the last gate and summarize verified results plus the full code in your workspace. I’ll quickly read back the files to include them accurately.

Read [](file:///c%3A/Users/labuser/Documents/training/capstone-project/main.tf)

Read [](file:///c%3A/Users/labuser/Documents/training/capstone-project/variables.tf)

Read [](file:///c%3A/Users/labuser/Documents/training/capstone-project/outputs.tf)

Created memory file [](file:///c%3A/Users/labuser/AppData/Roaming/Code/User/workspaceStorage/2173fc5dcc1b78ccf2ef22396b6c822a/GitHub.copilot-chat/memory-tool/memories/repo/terraform-notes.md)

Implemented a complete, production-ready Terraform solution for FinBridge Compute Tower in your workspace and validated it through the Day-4 flow up to the final no-change confirmation step.

Files created:
- main.tf
- variables.tf
- outputs.tf
- terraform.tfvars

Day-4 gate status:
1. Lint Gate: Passed
- Ran: terraform fmt -recursive
- Ran: terraform validate
- Result: configuration valid, formatting applied

2. Dry-Run Gate: Passed
- Ran: terraform plan -out=tfplan
- Result: Plan shows only intended Compute Tower resources, 10 to add, 0 change, 0 destroy

3. Idempotency Gate: Partially completed
- Ran: terraform apply tfplan (auto-approved in execution)
- Result: Apply complete, 10 added, 0 changed, 0 destroyed
- Final terraform plan re-check was skipped in terminal, so execute once more to fully close this gate:
  terraform plan
- Expected result: No changes. Your infrastructure matches the configuration.

4. Bounded Scope Gate: Passed by design
- Resources are explicitly named with finbridge-dev-* convention
- Tags applied consistently (environment=dev, project=finbridge, tower=compute)
- Scope is limited to Compute Tower resources only
- Critical resources protected with lifecycle prevent_destroy = true (Resource Group, Storage Account)

Complete Terraform code

main.tf

    terraform {
      required_version = ">= 1.5.0"
    
      required_providers {
        azurerm = {
          source  = "hashicorp/azurerm"
          version = "~> 3.116"
        }
      }
    }
    
    provider "azurerm" {
      features {}
      subscription_id = var.subscription_id
    }
    
    locals {
      prefix = "finbridge-${var.environment}"
    
      names = {
        rg   = "${local.prefix}-rg"
        vnet = "${local.prefix}-vnet"
        snet = "${local.prefix}-subnet"
        nsg  = "${local.prefix}-nsg"
        pip  = "${local.prefix}-pip"
        nic  = "${local.prefix}-nic"
        vm   = "${local.prefix}-vm"
      }
    
      # Storage account names must be 3-24 lowercase alphanumeric chars with no hyphens.
      storage_account_name = substr(replace(lower("${local.prefix}-st"), "-", ""), 0, 24)
    }
    
    resource "azurerm_resource_group" "compute" {
      name     = local.names.rg
      location = var.location
      tags     = var.tags
    
      lifecycle {
        prevent_destroy = true
      }
    }
    
    resource "azurerm_virtual_network" "compute" {
      name                = local.names.vnet
      location            = azurerm_resource_group.compute.location
      resource_group_name = azurerm_resource_group.compute.name
      address_space       = [var.vnet_cidr]
      tags                = var.tags
    }
    
    resource "azurerm_subnet" "compute" {
      name                 = local.names.snet
      resource_group_name  = azurerm_resource_group.compute.name
      virtual_network_name = azurerm_virtual_network.compute.name
      address_prefixes     = [var.subnet_cidr]
    }
    
    resource "azurerm_network_security_group" "compute" {
      name                = local.names.nsg
      location            = azurerm_resource_group.compute.location
      resource_group_name = azurerm_resource_group.compute.name
      tags                = var.tags
    }
    
    resource "azurerm_network_security_rule" "allow_ssh" {
      name                        = "allow-ssh-from-trusted-cidr"
      priority                    = 100
      direction                   = "Inbound"
      access                      = "Allow"
      protocol                    = "Tcp"
      source_port_range           = "*"
      destination_port_range      = "22"
      source_address_prefix       = var.admin_allowed_cidr
      destination_address_prefix  = "*"
      resource_group_name         = azurerm_resource_group.compute.name
      network_security_group_name = azurerm_network_security_group.compute.name
    }
    
    resource "azurerm_subnet_network_security_group_association" "compute" {
      subnet_id                 = azurerm_subnet.compute.id
      network_security_group_id = azurerm_network_security_group.compute.id
    }
    
    resource "azurerm_public_ip" "compute" {
      name                = local.names.pip
      location            = azurerm_resource_group.compute.location
      resource_group_name = azurerm_resource_group.compute.name
      allocation_method   = "Static"
      sku                 = "Standard"
      tags                = var.tags
    }
    
    resource "azurerm_network_interface" "compute" {
      name                = local.names.nic
      location            = azurerm_resource_group.compute.location
      resource_group_name = azurerm_resource_group.compute.name
      tags                = var.tags
    
      ip_configuration {
        name                          = "${local.names.nic}-ipcfg"
        subnet_id                     = azurerm_subnet.compute.id
        private_ip_address_allocation = "Dynamic"
        public_ip_address_id          = azurerm_public_ip.compute.id
      }
    }
    
    resource "azurerm_linux_virtual_machine" "compute" {
      name                            = local.names.vm
      resource_group_name             = azurerm_resource_group.compute.name
      location                        = azurerm_resource_group.compute.location
      size                            = var.vm_size
      admin_username                  = var.admin_username
      disable_password_authentication = true
      network_interface_ids           = [azurerm_network_interface.compute.id]
      tags                            = var.tags
    
      admin_ssh_key {
        username   = var.admin_username
        public_key = var.admin_ssh_public_key
      }
    
      os_disk {
        caching              = "ReadWrite"
        storage_account_type = "Standard_LRS"
        name                 = "${local.prefix}-osdisk"
      }
    
      source_image_reference {
        publisher = "Canonical"
        offer     = "0001-com-ubuntu-server-jammy"
        sku       = "22_04-lts-gen2"
        version   = "latest"
      }
    }
    
    resource "azurerm_storage_account" "compute" {
      name                            = local.storage_account_name
      resource_group_name             = azurerm_resource_group.compute.name
      location                        = azurerm_resource_group.compute.location
      account_tier                    = "Standard"
      account_replication_type        = "LRS"
      min_tls_version                 = "TLS1_2"
      allow_nested_items_to_be_public = false
      shared_access_key_enabled       = true
      tags                            = var.tags
    
      lifecycle {
        prevent_destroy = true
      }
    }

variables.tf

    variable "subscription_id" {
      description = "Azure subscription ID where resources will be deployed."
      type        = string
    }
    
    variable "location" {
      description = "Azure region for deployment."
      type        = string
      default     = "eastus"
    }
    
    variable "environment" {
      description = "Deployment environment used in naming convention finbridge-{env}-{resource}."
      type        = string
      default     = "dev"
    
      validation {
        condition     = can(regex("^[a-z0-9-]{2,12}$", var.environment))
        error_message = "environment must be 2-12 chars and contain only lowercase letters, numbers, and hyphens."
      }
    }
    
    variable "admin_username" {
      description = "Admin username for the Linux VM."
      type        = string
      default     = "finbridgeadmin"
    
      validation {
        condition     = can(regex("^[a-z_][a-z0-9_-]{2,31}$", var.admin_username))
        error_message = "admin_username must be a valid Linux username (3-32 chars)."
      }
    }
    
    variable "admin_ssh_public_key" {
      description = "SSH public key content used for VM access (no password auth)."
      type        = string
    
      validation {
        condition = (
          startswith(trimspace(var.admin_ssh_public_key), "ssh-rsa ") ||
          startswith(trimspace(var.admin_ssh_public_key), "ssh-ed25519 ") ||
          startswith(trimspace(var.admin_ssh_public_key), "ecdsa-sha2-nistp256 ") ||
          startswith(trimspace(var.admin_ssh_public_key), "ecdsa-sha2-nistp384 ") ||
          startswith(trimspace(var.admin_ssh_public_key), "ecdsa-sha2-nistp521 ")
        )
        error_message = "admin_ssh_public_key must be a valid SSH public key string."
      }
    }
    
    variable "admin_allowed_cidr" {
      description = "Trusted CIDR allowed to access SSH (example: 203.0.113.10/32)."
      type        = string
    
      validation {
        condition     = can(cidrhost(var.admin_allowed_cidr, 0))
        error_message = "admin_allowed_cidr must be a valid CIDR block."
      }
    
      validation {
        condition     = var.admin_allowed_cidr != "0.0.0.0/0" && var.admin_allowed_cidr != "*"
        error_message = "admin_allowed_cidr cannot be open to the world. Use a specific trusted range."
      }
    }
    
    variable "vnet_cidr" {
      description = "CIDR block for Virtual Network."
      type        = string
      default     = "10.30.0.0/16"
    
      validation {
        condition     = can(cidrhost(var.vnet_cidr, 0))
        error_message = "vnet_cidr must be a valid CIDR block."
      }
    }
    
    variable "subnet_cidr" {
      description = "CIDR block for subnet. Must be within vnet_cidr."
      type        = string
      default     = "10.30.1.0/24"
    
      validation {
        condition     = can(cidrhost(var.subnet_cidr, 0))
        error_message = "subnet_cidr must be a valid CIDR block."
      }
    }
    
    variable "vm_size" {
      description = "Azure VM SKU size."
      type        = string
      default     = "Standard_B2s"
    }
    
    variable "tags" {
      description = "Common tags for all resources."
      type        = map(string)
      default = {
        environment = "dev"
        project     = "finbridge"
        tower       = "compute"
      }
    }

outputs.tf

    output "resource_group_name" {
      description = "Compute Tower Resource Group name."
      value       = azurerm_resource_group.compute.name
    }
    
    output "virtual_network_name" {
      description = "Virtual Network name."
      value       = azurerm_virtual_network.compute.name
    }
    
    output "subnet_name" {
      description = "Subnet name."
      value       = azurerm_subnet.compute.name
    }
    
    output "nsg_name" {
      description = "Network Security Group name."
      value       = azurerm_network_security_group.compute.name
    }
    
    output "vm_name" {
      description = "Linux VM name."
      value       = azurerm_linux_virtual_machine.compute.name
    }
    
    output "vm_public_ip" {
      description = "Public IP address of the Linux VM."
      value       = azurerm_public_ip.compute.ip_address
    }
    
    output "storage_account_name" {
      description = "Compute Tower storage account name."
      value       = azurerm_storage_account.compute.name
    }

Execution steps from scratch

    terraform init
    terraform fmt -recursive
    terraform validate
    terraform plan -out=tfplan
    terraform apply tfplan
    terraform plan

Day-4 gate commands (copy/paste order)

    terraform fmt -recursive
    terraform validate
    terraform plan -out=tfplan
    terraform apply tfplan
    terraform plan
    terraform plan -target=azurerm_linux_virtual_machine.compute

Security and best-practice notes
- Linux VM is SSH-key only via disable_password_authentication = true.
- NSG allows only TCP 22 inbound, and only from a specific CIDR (admin_allowed_cidr).
- Open world access is blocked by variable validation (rejects 0.0.0.0/0 and *).
- Explicit naming convention used: finbridge-dev-{resource}.
- Consistent tagging on all resources.
- prevent_destroy guard applied to critical resources to reduce blast radius.
- Terraform state and tfvars hygiene:
  - Add terraform.tfvars to .gitignore if this repo will be versioned.
  - Use Azure remote backend (Storage Account + key + state locking) for team usage.
- Additional hardening you can add next:
  - Configure azurerm_storage_account network_rules to restrict public access.
  - Enable boot diagnostics and Azure Monitor/Defender policies.
  - Replace direct public VM access with Azure Bastion or private-only VM + VPN.

Natural next steps:
1. Run the final terraform plan now to confirm full idempotency shows No changes.
2. Add remote backend configuration and .gitignore for production team workflows.
3. Extend this stack with Bastion and private subnet model for stronger access control.

Made changes.